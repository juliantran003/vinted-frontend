import { Link } from "react-router-dom";
import Header from "../components/Header";

const Offer = (props) => {
  return (
    <div>
      <Header />
      <Link to="/">Home</Link>
    </div>
  );
};

export default Offer;
